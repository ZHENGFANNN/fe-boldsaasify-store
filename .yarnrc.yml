# CF Workers Builds 默认用 Yarn 4 + CI immutable 安装，会因 package.json 改动
# 与旧 Yarn1 lockfile 不一致而报 YN0028。这里关掉 immutable，让安装可写 lockfile；
# 并强制 node-modules 布局（Yarn 4 默认 PnP 会让 Next/OpenNext 构建失败）。
nodeLinker: node-modules
enableImmutableInstalls: false
